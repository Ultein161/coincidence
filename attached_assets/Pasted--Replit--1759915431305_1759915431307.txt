Отлично! Ниже — адаптированный **промт специально для Replit**, с учётом особенностей этой платформы (простота запуска, изоляция фронта и бэка, отсутствие полноценного HTTPS, удобство для тестирования).

Он подойдёт для использования **внутри Replit**, где фронтенд и бэкенд развернуты как два отдельных проекта или Repl’а.

---

### ✅ ПРОМПТ ДЛЯ REPLIT: Реализация JWT-авторизации на фронтенде

---

**Ты — опытный фронтенд-разработчик, работающий в среде Replit.**
У тебя уже есть готовое приложение с **отдельным фронтендом и бэкендом**, реализованы **регистрация и авторизация**, а также настроен обмен JWT-токенами (токен выдается при логине).

Теперь необходимо доработать **фронтенд-часть**, чтобы корректно обрабатывать JWT. Важно, чтобы ChatGPT строго следовал описанным шагам и не упускал деталей. В Replit важно, чтобы всё было просто, прозрачно и понятно для запуска.

---

### 🔧 **Что нужно реализовать:**

1. ✅ **После авторизации сохранять JWT** — использовать `localStorage` (или `sessionStorage` — ты должен обосновать выбор).
2. ✅ **Автоматически прикреплять JWT** к каждому запросу на бэкенд (в заголовок `Authorization: Bearer <token>`).
3. ✅ **Создать универсальный обработчик API-запросов** — например, `apiClient`, чтобы не прописывать токен вручную каждый раз.
4. ✅ **Скрыть страницы логина и регистрации** для уже авторизованных пользователей — перенаправлять их, например, на `/dashboard`.
5. ✅ **Добавить проверку токена на клиенте** — использовать `jwt-decode` (или аналог) для проверки валидности токена и даты истечения.
6. ✅ **Реализовать logout** — удалять токен и делать редирект на `/login`.
7. ✅ **Добавить защиту маршрутов** — создать компонент типа `<PrivateRoute>` или обернуть `Route`-ы, чтобы запретить доступ к защищённым страницам без токена.

---

### ⚙️ **Технические требования:**

* Проект должен работать в Replit без настройки CI/CD.
* Используй **React** (с хуками). Можно использовать Context API или Zustand для глобального состояния авторизации.
* Реализация универсального API-запроса может быть через `fetch` или `axios`.
* Обработка ошибок (например, если токен истёк) должна быть централизованной.
* Предусмотри, что адрес бэкенда может быть другим Repl’ом (указать базовый URL, например: `https://backend-project.username.repl.co/api`).

---

### 🧠 **Тон ответов ChatGPT:**

* Спокойный, технический, последовательный.
* Объясняй решения: почему `localStorage`, почему `axios`, как защищать маршруты.
* Приводи примеры кода (небольшие), но с пояснениями.

---

### 📐 **Структура ответа ChatGPT:**

1. Краткое пояснение задачи и логики JWT на фронте.
2. Выбор между `localStorage` и `sessionStorage`.
3. Создание API-клиента (`apiClient.js` или `useApi.js`).
4. Реализация логина: получение токена, сохранение, редирект.
5. Проверка токена (валидность, истечение).
6. Реализация logout.
7. Обработка защищённых маршрутов (`PrivateRoute`).
8. Перенаправление авторизованных пользователей с `/login` и `/register`.
9. Обработка ошибок: токен невалиден или истёк.
10. Структура проекта (файлы, папки).
11. Подсказки по отладке в Replit.
12. Возможные улучшения (refresh-токены, защита от XSS).
13. Заключение: советы по безопасности и дальнейшему расширению.

---

### 💡 Советы по Replit:

* Проверь CORS на бэке: бэкенд должен принимать запросы с домена фронта.
* Используй `.env` или файл `config.js` для указания адреса бэкенда.
* Храни токен **только в `localStorage`**, так как в Replit не работают `httpOnly`-cookies по умолчанию.

---

Если нужно, я могу также:

* Добавить пример структуры проекта в Replit;
* Написать шаблон файлов (`apiClient.js`, `AuthContext.js`, `PrivateRoute.jsx`);
* Подготовить «минимальный стартовый проект».

Сообщите, если хотите это — и я добавлю!
